```console
help                  print help
all                   combo: clean, resolve deps and run wgen
build                 compile wgen
clean                 clean and remove wgen binary
run                   compile and run wgen
deps                  resolve dependencies
fmt                   format all
```
